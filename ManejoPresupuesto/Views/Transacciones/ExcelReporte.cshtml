@{
    ViewData["Title"] = "Reporte Excel";
    var hoy = DateTime.Now;
    var haceUnMes = hoy.AddMonths(-1);
    var mesActual = hoy.Month;
    var añoActual = hoy.Year;
    var mesAnterior = haceUnMes.Month;
    var mesAnteriorAño = haceUnMes.Year;
    var añoAnterior = añoActual - 1;

}
<partial name="_SubMenu" Model="SubMenuTransacciones.ExcelReporte" />
<h1>Exportar transacciones a Excel</h1>

<div class="mb-3">
    <a class="btn btn-success" asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-año="@añoActual">Exportar mes actual</a> ~ @hoy.ToString("MMMM yyyy")
</div>
<div class="mb-3">
    <a class="btn btn-success" asp-action="ExportarExcelPorMes" asp-route-mes="@mesAnterior" asp-route-año="@mesAnteriorAño">Exportar mes anterior</a> ~ @haceUnMes.ToString("MMMM yyyy")
</div>
<div class="mb-3">
    <a class="btn btn-success" asp-action="ExportarExcelPorAño" asp-route-año="@añoActual">Exportar año actual</a> ~ @añoActual
</div>
<div class="mb-3">
    <a class="btn btn-success" asp-action="ExportarExcelPorAño"  asp-route-año="@añoAnterior">Exportar año anterior</a> ~ @añoAnterior
</div>
<div class="mb-3">
    <a class="btn btn-success" asp-action="ExportarExcelTodo">Exportar todo</a> 
</div>
